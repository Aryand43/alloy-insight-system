# Point the dashboard at the local data bridge (npm run dev starts both).
# Set VITE_USE_MOCK=true to run the UI standalone on demo data instead.
VITE_USE_MOCK=false
VITE_API_BASE_URL=http://localhost:3001
