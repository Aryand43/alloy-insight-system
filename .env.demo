# Hosted demo (`vite build --mode demo`): the API is the same-origin Vercel
# function under /api, reading the demo-data/ subset.
VITE_USE_MOCK=false
VITE_API_BASE_URL=/api
VITE_DEMO_NOTE="Demo subset — thermal frames for 60105609r5 only (layers 1, 2, 28, 56; every 3rd frame). Process Insight covers all 26 builds."
